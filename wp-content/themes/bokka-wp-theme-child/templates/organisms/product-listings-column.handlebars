<div class="listings-wrap product-tabs">
    <div class="tab-header-wrap">
        <div class="tab-header">
            {{#each tabs}}
            <div class="product-tab-header">
                <h2>{{title}}</h2>
                <p>{{copy}}</p>
            </div>
            {{/each}}
        </div>
        <div class="product-tab-links">
            <ul>
                {{#each tabs}}
                    <li class="tab {{class}} {{#if products}}active{{/if}}"><a href="#">{{tab_title}}</a></li>
                {{/each}}
            </ul>
        </div>
    </div>
    <ul class="product-tab-bodies">
        {{#each tabs}}
            <li class="tab-body">
                {{!--- THIS IS FOR NEIGHBORHOODS--}}
                {{#if products}}
                    <ul>
                        {{#each products}}
                            {{> molecules/blocks/overview-card.handlebars}}
                        {{/each}}
                    </ul>
                {{/if}}
                {{!-- THIS IS FOR QMI & MODELS --}}
                {{#if neighborhoods}}
                    <ul>
                        {{#each neighborhoods}}
                            <li>
                                <h3><span>{{title}}</span></h3>
                                <ul>
                                    {{#each products}}
                                        {{> molecules/blocks/overview-card.handlebars}}
                                    {{/each}}
                                </ul>
                            </li>
                        {{/each}}
                    </ul>
                {{/if}}
            </li>
        {{/each}}
    </ul>
    <div class="more">
        <a href="/our-homes/view-homes/" class="button purple">See Quick Move-ins</a>
        <a href="/our-homes/view-model-homes/" class="button purple">See Model Homes</a>
    </div>
</div><!--/listings-wrapper-->
